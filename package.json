{
  "name": "droit-de-vivre-com",
  "version": "1.1.0",
  "description": "Droit-de-vivre.com Jekyll website",
	"author": "Team Ninjas",
	"scripts": {
    "dev": "docker run --rm --volume=${PWD}:/srv/jekyll -p 4000:4000 -it jekyll/jekyll jekyll serve",
    "test": "echo \"Error: no test specified\" && exit 1"
	},
	"dependencies": {

	},
	"devDependencies": {
	}
}